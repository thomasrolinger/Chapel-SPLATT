var length: int = 5;
var A: [1..length] string = ("sudo", "make", "me", "a", "sandwich");

// Iterates through the numbers 1, 2, 3
for i in {1..3} {
    write(i, " ");
}

// Foreach loop
for a in A {
    write(a, " ");
}
